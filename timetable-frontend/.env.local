# Local-only overrides (Vite loads this AFTER .env — higher priority on localhost).
# Point directly at the Node backend (npm run start:dev → port 9000).
VITE_API_BASE_URL=http://localhost:9000/api/v1
VITE_API_TIMEOUT=60000
VITE_NODE_ENV=development
