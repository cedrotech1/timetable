# Optional now — only if you prefer Apache ProxyPass instead of api-proxy.php.
# For "copy folder to htdocs with no settings", use:
#   prepare-for-htdocs.bat + START-BACKEND.bat + .htaccess + api-proxy.php
# See HOW-TO-HTDOCS.txt in the project root.
